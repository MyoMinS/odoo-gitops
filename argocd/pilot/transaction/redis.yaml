apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pilot-transaction-redis
spec:
  project: default
  source:
    repoURL: https://gitlab.example.com/odoo-gitops.git
    targetRevision: main
    path: charts/redis
    helm:
      valueFiles:
      - ../../env/pilot/transaction-redis.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: pilot-transaction-redis
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
